{
  "buildCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter 2>&1 || true; fi && export PATH=\"$PATH:$HOME/flutter/bin\" && flutter --version 2>&1 && flutter pub get 2>&1 && flutter build web --release 2>&1",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Installing Flutter...'"
}

